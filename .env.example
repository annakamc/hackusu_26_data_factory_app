DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_TOKEN=your_personal_access_token
DATABRICKS_WAREHOUSE_ID=your_sql_warehouse_id
DATABRICKS_CATALOG=main
DATABRICKS_SCHEMA=predictive_maintenance
GENIE_SPACE_ID=
BEDROCK_REGION=us-east-1
USE_MOCK_AI=false
# Optional, local dev only: set to true to skip SSL verify if you get certificate errors (e.g. behind VPN)
# DATABRICKS_INSECURE_TLS=false
# If on campus/corporate proxy (e.g. guest.usu.edu), bypass proxy for Databricks so KPIs/Genie work:
# NO_PROXY=*.databricks.com,*.cloud.databricks.com
DEV_USER_EMAIL=dev@local
